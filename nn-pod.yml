apiVersion: v1
kind: Pod
metadata:
  name: k8s.kclproject.site
  labels:
    app: kcl-project
spec:
  containers:
  - name: kcl-project
    image: neo4j
    ports:
    - containerPort: 7474
      name: browser
    - containerPort: 7687
      name: bolt